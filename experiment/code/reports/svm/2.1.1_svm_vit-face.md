# Klasifikasi DemogPairs Menggunakan Fitur ViT (Wajah) dengan SVM

Dataset **DemogPairs** terdiri dari **10.800 gambar** wajah yang terbagi dalam **6 kelas interseksional** hasil kombinasi 3 kategori Ras (Asian, Black, White) × 2 kategori Gender (Males, Females): `Asian_Females`, `Asian_Males`, `Black_Females`, `Black_Males`, `White_Females`, `White_Males` (masing-masing 1.800 gambar, seimbang). Eksperimen ini menggunakan **fitur ViT (Vision Transformer) wajah** berdimensi 768 yang telah diekstraksi sebelumnya (`features/demogpairs_vit-face.pkl`), dan melakukan klasifikasi dengan **Support Vector Machine (SVC)** yang dioptimasi melalui **GridSearchCV dengan StratifiedKFold 5-fold** (`shuffle=True, random_state=42`, `refit='accuracy'`, scoring accuracy / f1_macro / precision_macro / recall_macro). Tujuan eksperimen adalah mencari kombinasi hyperparameter SVM terbaik dan mengevaluasi performa klasifikasi 6 kelas tersebut pada data uji yang belum pernah dilihat.

> Sumber notebook: `D:/Research/face-race-gender-multi-vit/experiment/code/2.1.1_svm_vit-face_demogpairs.ipynb` — semua angka di bawah diambil langsung dari `cell.outputs` notebook, tanpa modifikasi atau tebakan.

---

## 1. Konfigurasi Eksperimen

| Aspek | Nilai / Detail (dari output notebook) |
|---|---|
| **Jumlah fitur per gambar** | **768** — output cell *Load Fitur*: `Jumlah fitur per gambar: 768` |
| **Jumlah data** | **10.800** gambar (`pd.DataFrame` menampilkan `[10800 rows x 5 columns]`) |
| **Split data** | `train_test_split(test_size=0.2, stratify=y, random_state=42)` → **8.640 train / 2.160 test** (stratified 80/20). Output cell *Split Data*: `(8640, 2160)`. Test set seimbang: 360 sampel per kelas (terlihat dari kolom Support). |
| **Pipeline** | `Pipeline([('scaler', None), ('pca', None), ('classifier', None)])` — scaler dan PCA bersifat opsional |
| **Cross-validation** | `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)` |
| **Scoring** | `accuracy`, `f1_macro`, `precision_macro`, `recall_macro` (refit = `accuracy`) |
| **Parallel** | `n_jobs = int(cpu_count * 0.6)`, `parallel_backend('threading')` |

### Grid Parameter (`grid_params`)

Diambil verbatim dari cell *Kombinasi Parameter*:

```python
grid_params = [
    {
        'scaler': [None, MinMaxScaler()],
        'pca': [None, PCA(n_components=0.5), PCA(n_components=0.75)],
        'classifier': [SVC()],
        'classifier__C': [0.01, 0.1, 1, 10],
        'classifier__kernel': ['rbf', 'poly', 'linear'],
        'classifier__gamma': ['scale', 'auto'],
        'classifier__degree': [2, 3],
        'classifier__tol': [1e-3],
        'classifier__probability': [True],
    },
]
```

Rincian:
- **scaler**: 2 opsi — `None` (tanpa scaling), `MinMaxScaler()`
- **pca**: 3 opsi — `None` (tanpa PCA), `PCA(n_components=0.5)`, `PCA(n_components=0.75)`
- **classifier**: `SVC()`
- **C** (regularisasi): 4 nilai — `0.01, 0.1, 1, 10`
- **kernel**: 3 jenis — `rbf, poly, linear`
- **gamma**: 2 nilai — `scale, auto`
- **degree** (untuk kernel poly): 2 nilai — `2, 3`
- **tol**: `1e-3`
- **probability**: `True`

### Jumlah Kombinasi Parameter

Output cell *Kombinasi Parameter*:

```
SVC: 288 kombinasi
```

> Perhitungan: 2 × 3 × 4 × 3 × 2 × 2 × 1 × 1 = 288. Setiap kombinasi dievaluasi dengan 5-fold CV, sehingga total 1.440 fitting + 1 refit pada seluruh data train untuk model terbaik.

---

## 2. Parameter Terbaik

Output cell *Klasifikasi* — bagian **Best Parameters** (`evaluation_results` / `fold_results`):

```python
{
    'classifier': 'SVC',
    'classifier__C': 10,
    'classifier__degree': 2,
    'classifier__gamma': 'scale',
    'classifier__kernel': 'rbf',
    'classifier__probability': True,
    'classifier__tol': 0.001,
    'pca': None,
    'scaler': None
}
```

| Parameter | Nilai Terbaik |
|---|---|
| `classifier` | `SVC` |
| `classifier__C` | `10` |
| `classifier__degree` | `2` |
| `classifier__gamma` | `scale` |
| `classifier__kernel` | `rbf` |
| `classifier__probability` | `True` |
| `classifier__tol` | `0.001` |
| `pca` | `None` |
| `scaler` | `None` |

Interpretasi: model terbaik adalah SVM dengan kernel RBF, C=10, gamma=scale, tanpa scaler dan tanpa PCA (fitur ViT 768 dimensi dipakai mentah).

File model tersimpan: `models/clf_demogpairs_svm_vit-face_SVC.pkl` (terlihat di tabel ringkasan `model_file_path`).

---

## 3. Waktu Pelatihan

### 3.1 Waktu Pelatihan (Jobs)

Output cell `u.load_object(...) / Waktu Pelatihan (Jobs)` — nilai `training_time` yang dikembalikan `u.evaluate_models` (wall-clock gabungan job paralel):

```python
{
    'input_seconds': 10277.0,
    'days': 0,
    'hours': 2,
    'minutes': 51,
    'seconds': 17.0,
    'text': '0 hari 2 jam 51 menit 17.0 detik'
}
```

**0 hari 2 jam 51 menit 17.0 detik** (10.277 detik).

### 3.2 Waktu Pelatihan (Total Fold + Refit)

Output cell `Waktu Pelatihan` — dihitung sebagai `sum(fold_results[i]['Train Time Mean'] * 5) + model.refit_time_`:

```python
{
    'input_seconds': 83703.0,
    'days': 0,
    'hours': 23,
    'minutes': 15,
    'seconds': 3.0,
    'text': '0 hari 23 jam 15 menit 3.0 detik'
}
```

**0 hari 23 jam 15 menit 3.0 detik** (83.703 detik).

> Perbedaan: 3.1 adalah waktu wall-clock paralel (n_jobs ≈ 60% CPU), 3.2 adalah penjumlahan waktu train tiap fold (sekuensial ekivalen) ditambah waktu refit final.

---

## 4. Hasil Evaluasi

Evaluasi dilakukan pada **test set 2.160 gambar** (360 per kelas) menggunakan model terbaik yang telah di-refit pada seluruh data train.

### 4.1 Confusion Matrix

Output stream text cell *Klasifikasi* — `Confusion Matrix:` (gambar juga disimpan di `images/cm_svm_vit-face_SVC.png`):

> Baris = label sebenarnya (True), Kolom = prediksi (Predicted). Urutan kelas sesuai header: `Asian_Females, Asian_Males, Black_Females, Black_Males, White_Females, White_Males`.

|  | **Asian_Females** | **Asian_Males** | **Black_Females** | **Black_Males** | **White_Females** | **White_Males** |
|---|---|---|---|---|---|---|
| **Asian_Females** | **333** | 1 | 10 | 6 | 10 | 0 |
| **Asian_Males** | 0 | **325** | 2 | 5 | 8 | 20 |
| **Black_Females** | 6 | 1 | **330** | 12 | 3 | 8 |
| **Black_Males** | 6 | 4 | 10 | **340** | 0 | 0 |
| **White_Females** | 9 | 15 | 6 | 1 | **320** | 9 |
| **White_Males** | 0 | 16 | 12 | 2 | 16 | **314** |

Verifikasi diagonal (benar per kelas): 333 + 325 + 330 + 340 + 320 + 314 = 1.962 dari 2.160 → akurasi 0.9083 (konsisten dengan metrik global).

Pola kesalahan dominan:
- `Asian_Males` ↔ `White_Males` (20 salah), `Asian_Males` → `White_Females` (8)
- `White_Males` → `Asian_Males` (16), `White_Males` → `White_Females` (16)
- `White_Females` → `Asian_Males` (15)

### 4.2 Metrik Per Kelas

Output `display_table` HTML per-kelas (6 baris) — nilai diambil verbatim, presisi penuh dari HTML, dibulatkan 4 desimal untuk keterbacaan:

| Kelas | OvR Accuracy | Precision | Recall | F1-Score | Support |
|---|---|---|---|---|---|
| Asian_Females | 0.9778 (0.9777777777777777) | 0.9407 (0.940677966101695) | 0.9250 (0.925) | 0.9328 (0.9327731092436976) | 360 |
| Asian_Males | 0.9667 (0.9666666666666667) | 0.8978 (0.8977900552486188) | 0.9028 (0.9027777777777778) | 0.9003 (0.9002770083102494) | 360 |
| Black_Females | 0.9676 (0.9675925925925926) | 0.8919 (0.8918918918918919) | 0.9167 (0.9166666666666666) | 0.9041 (0.9041095890410958) | 360 |
| Black_Males | 0.9787 (0.9787037037037037) | 0.9290 (0.9289617486338798) | 0.9444 (0.9444444444444444) | 0.9366 (0.9366391184573003) | 360 |
| White_Females | 0.9644 (0.9643518518518519) | 0.8964 (0.896358543417367) | 0.8889 (0.8888888888888888) | 0.8926 (0.8926080892608089) | 360 |
| White_Males | 0.9616 (0.961574074074074) | 0.8946 (0.8945868945868946) | 0.8722 (0.8722222222222222) | 0.8833 (0.8832630098452883) | 360 |

> OvR Accuracy = akurasi one-vs-rest per kelas.

Kelas terbaik: **Black_Males** (F1 0.9366, Recall 0.9444) dan **Asian_Females** (Precision 0.9407, F1 0.9328). Kelas tersulit: **White_Males** (Recall 0.8722, F1 0.8833).

### 4.3 Metrik Global

Output stream `Test Results` + `classification_report`:

```
Accuracy  : 0.9083333333333333
Precision : 0.9083778499800578
Recall    : 0.9083333333333332
F1 Score  : 0.9082783206930735

               precision    recall  f1-score   support

Asian_Females     0.9407    0.9250    0.9328       360
  Asian_Males     0.8978    0.9028    0.9003       360
Black_Females     0.8919    0.9167    0.9041       360
  Black_Males     0.9290    0.9444    0.9366       360
White_Females     0.8964    0.8889    0.8926       360
  White_Males     0.8946    0.8722    0.8833       360

     accuracy                         0.9083      2160
    macro avg     0.9084    0.9083    0.9083      2160
 weighted avg     0.9084    0.9083    0.9083      2160
```

| Metrik Global | Nilai |
|---|---|
| **Accuracy** | **0.9083** (0.9083333333333333) |
| **Precision (macro avg)** | **0.9084** (0.9083778499800578) |
| **Recall (macro avg)** | **0.9083** (0.9083333333333332) |
| **F1-Score (macro avg)** | **0.9083** (0.9082783206930735) |
| Weighted avg Precision / Recall / F1 | 0.9084 / 0.9083 / 0.9083 (identik karena support seimbang) |

Tabel ringkasan `evaluation_results` (output display HTML terakhir cell Klasifikasi) juga mencatat:

| model_name | model_file_path | best_parameters | test_accuracy | test_f1 | test_precision | test_recall | parameter_combinations |
|---|---|---|---|---|---|---|---|
| SVC | models/clf_demogpairs_svm_vit-face_SVC.pkl | {'classifier': 'SVC', 'classifier__C': 10, ...} | 0.9083333333333333 | 0.9082783206930735 | 0.9083778499800578 | 0.9083333333333332 | 288 |

---

## 5. Hasil Cross-Validation

Output cell `fold_displays` — `u.display_table(fold_displays, n_items=[3, 3, 3, 3], ...)` menampilkan **Mean Accuracy** dari 5-fold CV untuk setiap kombinasi hyperparameter, diurutkan descending berdasarkan Mean. Parameter `n_items=[3, 3, 3, 3]` menyebabkan tabel ditampilkan **terpotong (truncated)** dengan baris `...` sebagai pemisah — hanya sebagian baris yang dirender di HTML. Di bawah ini adalah **seluruh baris yang tersedia di output notebook** (15 baris data + 3 baris ellipsis), direproduksi verbatim apa adanya. **Bukan** ringkasan sepihak — ini adalah konten lengkap dari `text/html` output cell 14.

> Catatan: kolom `Params` telah disederhanakan di notebook (`classifier__` dihapus, `classifier` dihapus). Nilai Fold dibulatkan 4 desimal di HTML. Mean = rata-rata 5 fold.

| No | Params | Fold 1 | Fold 2 | Fold 3 | Fold 4 | Fold 5 | Mean |
|---|---|---|---|---|---|---|---|
| 1 | C=10, degree=3, gamma=scale, kernel=rbf, probability=True, tol=0.001, pca=None, scaler=None | 0.9265 | 0.9120 | 0.9039 | 0.9103 | 0.9178 | 0.9141 |
| 2 | C=10, degree=2, gamma=scale, kernel=rbf, probability=True, tol=0.001, pca=None, scaler=None | 0.9265 | 0.9120 | 0.9039 | 0.9103 | 0.9178 | 0.9141 |
| 3 | C=10, degree=2, gamma=scale, kernel=rbf, probability=True, tol=0.001, pca=None, scaler=MinMaxScaler | 0.9225 | 0.9132 | 0.9039 | 0.9132 | 0.9172 | 0.9140 |
| ... | ... | ... | ... | ... | ... | ... | ... |
| 96 | C=10, degree=3, gamma=auto, kernel=linear, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.8906 | 0.8767 | 0.8756 | 0.8796 | 0.8808 | 0.8807 |
| 97 | C=10, degree=2, gamma=auto, kernel=linear, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.8906 | 0.8767 | 0.8756 | 0.8796 | 0.8808 | 0.8807 |
| 98 | C=10, degree=2, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.8924 | 0.8762 | 0.8744 | 0.8825 | 0.8773 | 0.8806 |
| ... | ... | ... | ... | ... | ... | ... | ... |
| 191 | C=1, degree=2, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.8744 | 0.8501 | 0.8605 | 0.8553 | 0.8640 | 0.8609 |
| 192 | C=0.01, degree=3, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=None | 0.8698 | 0.8588 | 0.8542 | 0.8530 | 0.8652 | 0.8602 |
| 193 | C=0.1, degree=3, gamma=scale, kernel=linear, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.8709 | 0.8553 | 0.8530 | 0.8594 | 0.8617 | 0.8601 |
| ... | ... | ... | ... | ... | ... | ... | ... |
| 286 | C=0.01, degree=3, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.7332 | 0.7494 | 0.7367 | 0.7471 | 0.7436 | 0.7420 |
| 287 | C=0.1, degree=3, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.6690 | 0.6678 | 0.6701 | 0.6910 | 0.6730 | 0.6742 |
| 288 | C=0.01, degree=3, gamma=auto, kernel=poly, probability=True, tol=0.001, pca=PCA, scaler=MinMaxScaler | 0.6690 | 0.6678 | 0.6701 | 0.6910 | 0.6730 | 0.6742 |

**Keterangan tambahan terkait output fold_results:**

- Data mentah `fold_results` (288 entri) disimpan di variabel notebook dan dipakai untuk menghitung `Waktu Pelatihan` (section 3.2). Namun **tabel HTML yang dirender hanya menampilkan 12 baris data + 3 baris ellipsis** karena `n_items=[3,3,3,3]` — sisa 276 baris tidak dirender di output notebook, sehingga **tidak tersedia di output notebook** dalam bentuk HTML/text. Jika diperlukan analisis lengkap 288 baris, perlu membaca variabel `fold_results` atau file `results/demogpairs_svm_vit-face_*.pkl` secara programatik.
- Struktur tiap entri `fold_results` yang dipakai untuk perhitungan waktu mencakup kunci: `Params`, `Fold 1`–`Fold 5`, `Accuracy Mean`, `F1 Score Mean`, `Precision Mean`, `Recall Mean`, `Train Time Mean` — namun di tabel terpotong hanya `Params`, `Fold 1`–`5`, dan `Mean` (alias `Accuracy Mean`) yang ditampilkan.
- Rentang Mean Accuracy: **0.6742 (terendah, rank 287–288) hingga 0.9141 (tertinggi, rank 1–2)** — selisih ~0.24 menunjukkan sensitivitas performa terhadap hyperparameter, terutama kernel poly dengan C kecil dan PCA yang performanya anjlok.

---

## Catatan Reproduksibilitas

- **Seed**: `random_state=42` untuk split dan CV shuffle.
- **Stratifikasi**: train/test dan tiap fold CV stratified menjaga proporsi 6 kelas.
- **File fitur**: `features/demogpairs_vit-face.pkl` — 768 dimensi (ViT-Base, kemungkinan `google/vit-base-patch16-224` atau varian face-tuned).
- **Model**: `models/clf_demogpairs_svm_vit-face_SVC.pkl` berisi pipeline + `refit_time_`.
- **Hasil mentah**: `results/demogpairs_svm_vit-face_*` (disimpan oleh `u.evaluate_models`).
- **Visual**: `images/cm_svm_vit-face_SVC.png` (confusion matrix heatmap, 600px).

> Report ini self-contained: seluruh angka berasal dari output notebook yang dieksekusi; pembaca tidak perlu membuka `.ipynb` untuk memahami eksperimen, namun dapat merujuk ke notebook untuk verifikasi atau melanjutkan analisis fold lengkap.
